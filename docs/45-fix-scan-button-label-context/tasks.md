# Issue #45: Implementation Tasks

- [x] 1. `BookSearchResultPage` に `source` パラメータのテストを追加する
- [x] 2. `BookSearchResultPage` に `source` パラメータを追加し、ボタンのラベルとアイコンを動的に切り替える
- [x] 3. `app_router.dart` でクエリパラメータ `source` を `BookSearchResultPage` に渡す
- [x] 4. 遷移元ページで `source` クエリパラメータを追加する（バーコードスキャン、ISBN入力）
- [x] 5. 既存テストを修正し、全テストが通ることを確認する
